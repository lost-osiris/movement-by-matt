import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    discord: {
      main: '#5865F2',
    },
    mode: 'dark',
    white: {
      main: '#FFFFFF',
    },
  },
})

export default theme
